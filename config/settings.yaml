# Configuracion no-sensible del proyecto. Nada de secretos aqui (ver README,
# seccion "Secrets", para la convencion de Secret Manager de Fase 2).

gcp_project: data-prd-424213
bigquery_dataset: 03_BaseModel

reports_dir: config/reports
clients_dir: config/clients

# Bucket de aterrizaje de los reportes generados (CLI + UI), ver
# gcs_landing.py. Creado en europe-southwest1 (misma region que el dataset
# de BigQuery), con uniform bucket-level access.
trace_bucket: reporting-automation-trace

# Carpeta raiz de Drive para delivery (Fase 2). No configurada todavia --
# sin esto, GDriveDelivery falla con un mensaje claro en vez de intentar
# subir a ningun lado. Ver README seccion Roadmap/Fase 2.
gdrive_root_folder_id: null

# Carpeta raiz de Drive para el comando `ask` (preguntas libres, ver README
# seccion "Preguntas en lenguaje natural"). Deliberadamente separada de
# gdrive_root_folder_id para no mezclar exports ad-hoc con entregas de
# reportes de cliente. No configurada todavia -- mismo comportamiento que
# gdrive_root_folder_id (falla con mensaje claro si falta).
adhoc_gdrive_folder_id: null
