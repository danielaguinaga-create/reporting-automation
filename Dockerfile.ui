# Imagen del Cloud Run Service de la UI (Streamlit). Servicio separado del
# de Fase 3 (main_entrypoint) -- ver Dockerfile para ese.
FROM python:3.13-slim

WORKDIR /app

# Deps de sistema de WeasyPrint (renderizado de PDF/HTML via plantillas Jinja2).
RUN apt-get update && apt-get install -y --no-install-recommends \
    libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml ./
COPY src ./src
COPY config ./config

RUN pip install --no-cache-dir .

ENV PORT=8080
EXPOSE 8080

CMD streamlit run src/reporting_automation/ui_app.py \
    --server.port=$PORT --server.address=0.0.0.0 --server.headless=true
